{
  "packageManager": "pnpm@10.29.3",
  "name": "faithreach-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "services/*",
    "frontend",
    "shared"
  ],
  "devDependencies": {
    "turbo": "latest"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  }
}
